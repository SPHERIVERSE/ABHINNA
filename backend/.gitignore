# Dependencies
node_modules/

# Environment variables (CRITICAL)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Production Build (Compiled JS)
dist/
build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# OS generated files
.DS_Store
Thumbs.db

# Editor directories
.idea/
.vscode/
*.swp
*.swo

# Coverage directory used by tools like istanbul/jest
coverage/

# Prisma
# We DO want to commit migrations, but we might want to ignore SQLite files if we used them (we use Postgres, so this is just a safety measure)
*.sqlite
*.sqlite-journal
/src/generated/
/generated/

# Temporary files
tmp/
temp/